class: edu.cmu.lti.oaqa.bio.core.ie.overlap.OverlappingPassageFilter
limit: 1000
cross-opts:
  FilterMode: [FILTER_OVERLAP, FILTER_IDENTICAL]

persistence-provider: |
  inherit: jdbc.log.log-persistence-provider
